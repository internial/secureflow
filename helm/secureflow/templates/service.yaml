apiVersion: v1
kind: Service
metadata:
  name: {{ include "secureflow.fullname" . }}
  labels:
    {{- include "secureflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "secureflow.selectorLabels" . | nindent 4 }}
