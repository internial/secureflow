apiVersion: v1
kind: Service
metadata:
  name: secureflow
  namespace: secureflow
  labels:
    app.kubernetes.io/name: secureflow
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: secureflow
